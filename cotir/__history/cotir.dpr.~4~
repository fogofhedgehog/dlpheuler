program cotir;

uses
  Vcl.Forms,
  jmpcotir in 'jmpcotir.pas' {Form1},
  NiApiLib_TLB in 'NiApiLib_TLB.pas',
  connect in 'connect.pas' {Form2};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TForm2, Form2);
  Application.Run;
end.
